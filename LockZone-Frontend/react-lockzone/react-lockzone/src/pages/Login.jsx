import { LoginComponent } from "../component";
import { Container } from "react-bootstrap";

export const Login = () => {
    return(
        <Container>
            <LoginComponent/>
        </Container>
    );
}